class HiveDatabase {
  /// refrece our hive

  /// check weather data is already stored if not ,record the start data
  /// return start date as yyyy/mm
  /// write data
  ///read data and return a List of workout
  /// check if any exercise have been done
}
